import type { Metadata } from 'next'
import './globals.css'

export const metadata: Metadata = {
  title: 'FarmSearch Edmonton',
  description: 'Discover local farm food, farmers markets, and organic stores near Edmonton.',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
